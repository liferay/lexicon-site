---
title: "Home"
description: "Something nice and compelling about the project. Make it short and sweet."
---

{namespace pageIndex}

/**
 * @param site
 */
{template .render}
	{call main.render data="all"}
		{param content kind="html"}
			{call Sidebar.render data="all"}
				{param section: $site.index.children['docs'] /}
			{/call}
			<div class="sidebar-offset">
				{call .header /}
			</div>
		{/param}
	{/call}
{/template}

/**
 *
 */
{template .header}
	<header class="header home">
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-12">
					<img src="/images/LexiconLogoHome.png" />
					<h1>Build consistent and beautiful web experiences easily</h1>
				</div>
			</div>
		</div>
	</header>
{/template}